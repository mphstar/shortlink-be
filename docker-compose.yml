version: '3.8'

services:
  backend:
    build: .
    container_name: shortlink-backend
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Since we use SQLite in the /app/data dir, mounting it ensures persistence.
